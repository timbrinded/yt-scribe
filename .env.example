# YTScribe Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# Required for core functionality
# =============================================================================

# OpenAI API key for transcription (Whisper) and chat (GPT-4o)
OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# Required for Google OAuth authentication
# =============================================================================

# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create or select a project
# 3. Go to APIs & Services > Credentials
# 4. Create an OAuth 2.0 Client ID (Web application type)
# 5. Add authorized redirect URI: http://localhost:3000/auth/google/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# =============================================================================
# Optional - Development defaults are fine for local development
# =============================================================================

# Backend server port (default: 3000)
# PORT=3000

# Database path (default: data/ytscribe.db)
# DATABASE_URL=/path/to/ytscribe.db

# OAuth redirect URI (default: http://localhost:3000/auth/google/callback)
# GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Frontend URL for OAuth redirect after login (default: http://localhost:4321)
# FRONTEND_URL=http://localhost:4321

# Log level: debug, info, warn, error (default: debug in dev, info in prod)
# LOG_LEVEL=debug

# Node environment (default: development)
# NODE_ENV=development

# =============================================================================
# Frontend environment variables (used by Astro)
# =============================================================================

# Backend API URL (default: http://localhost:3000)
# Must be prefixed with PUBLIC_ for client-side access in Astro
# PUBLIC_API_URL=http://localhost:3000
