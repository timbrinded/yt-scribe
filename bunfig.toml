[test]
# Guard against accidental 'bun test' usage.
# This project uses vitest + @effect/vitest — Bun's native runner is NOT supported.
# Always use 'bun run test' instead.
preload = ["./tests/bun-test-guard.ts"]
root = "./tests"
